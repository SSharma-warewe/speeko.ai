[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.portal"

[deploy]
# Railway public edge usually maps to PORT=8080; domain targetPort must match
startCommand = "sh -c 'serve -s dist -l tcp://0.0.0.0:${PORT:-8080}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
